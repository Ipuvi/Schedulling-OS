### CPU Schedulling 
##Since there can be multiple requests at any time, we wish to dedicate a fixed amount of time to every request
##so that everyone gets a fair share of the time. 
##We will log into the system from 10am to 12am only. 
##We wants to have separate requests queues for students and faculty, where faculty queue is given a higher priority.
##Here, Implemented a strategy for the same. The summary at the end of the session includes the total time spent on handling queries and average
query time.